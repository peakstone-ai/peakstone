# AGENTS.md — strict constraints (MANDATORY)

1. The solution file must contain **exactly one function, named exactly `process`**. No other
   top-level functions, classes, or code.
2. **No imports** of any kind.
3. The solution file must be **at most 25 lines**.
4. **Output format:** reply with ONLY the single ```python code block — no explanation, no prose
   before or after it.
