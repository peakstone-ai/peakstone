# Task

Implement `process(numbers)` in **`solution.py`**: given a list of integers, return the sum of
the squares of the **even** numbers. Example: `process([1, 2, 3, 4])` → `20` (2² + 4²).

Follow **all** constraints in AGENTS.md exactly.
