# Task

Implement **`solution.ts`** exporting two functions:

- `clamp(x: number, lo: number, hi: number)` — clamp `x` into the `[lo, hi]` range.
- `unique<T>(items: T[])` — return `items` with duplicates removed, preserving first-seen order.

Follow **all** conventions in AGENTS.md.
