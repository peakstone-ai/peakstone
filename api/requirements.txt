# Peakstone API dependencies (includes the engine's, since the API reuses engine/ for
# bundle hashing/validation + signature verification).
-r ../requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
