/models
/results/
__pycache__/
*.pyc
.pytest_cache/
node_modules/
peakstone.db
*.db
infra/.env
/build/
*.egg-info/
/dist/

# Imported benchmark corpora — regenerable via peakstone.engine.importers.* (kept out of
# git for size, and because LiveCodeBench problems are scraped from coding platforms).
/challenges/humaneval/
/challenges/bigcodebench/
/challenges/livecodebench/
/challenges/codeforces/
/challenges/aime/
/challenges/swebench/

# Claude Code per-machine local settings (permission allowlist etc.) — keep out of the repo
.claude/settings.local.json
